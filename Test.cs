using System;
using System.Collections.Generic;
using System.Text;

namespace Gra
{
    class Test // klasa dla każdego, tylko nie usuwajcie a bierzcie w komentarz
    {
        /*
        public static void test1()
        {
            ConsoleKeyInfo cki;
            do
            {
                Console.WriteLine(" ");
                Console.WriteLine("c czy otwarte");
                Console.WriteLine("z znalezienie");
                Console.WriteLine("q wyjscie");
                cki = Console.ReadKey();
                if (cki.Key == ConsoleKey.C)
                {
                    if (Program.czyKlucz() == 0) Console.WriteLine("nie masz klucza");
                    else Console.WriteLine("masz klucz");
                }
                if (cki.Key == ConsoleKey.Z) Program.znalezienieKlucza();
            } while (cki.Key != ConsoleKey.Q);
            System.Environment.Exit(0);
        } */
    }
}
